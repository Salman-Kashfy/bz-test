export enum Status {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
}

export enum GenderType {
    MALE = 'MALE',
    FEMALE = 'FEMALE',
    RATHER_NOT_SAY = 'RATHER_NOT_SAY',
    ANY = 'ANY',
}

export enum Roles {
    SUPER_ADMIN = 'Super Admin',
    ADMIN = 'Admin',
    HR_ADMIN = 'HR Manager',
    MANAGER = 'Team Lead',
    EMPLOYEE = 'Employee',
}